name: Deploy static Stitch site to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Prepare static site
        shell: bash
        run: |
          rm -rf _site
          mkdir _site
          cp index.html _site/index.html
          cp library.html _site/library.html
          cp article.html _site/article.html
          cp tags.html _site/tags.html
          cp DESIGN.md _site/DESIGN.md
          cp .nojekyll _site/.nojekyll
          cp -R stitch-screens _site/stitch-screens
          cp -R assets _site/assets

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
